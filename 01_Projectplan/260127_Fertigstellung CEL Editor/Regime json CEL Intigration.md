Ich habe einen 'CEL Editor ' mit dem ich Abläufe beim Trading automatisieren möchte, wie z.b. durch eine json Datei, die dazu da ist, Regimes im aktuellen Chart zu erkennen und durch die cel skript logik bei bestimmten erkannten regimes, einen trade aus zu lösen, stop loss und take profit zu setzen. Nun müsste ich an der json datei die Logik ändern mittels des CEL Editors, momentan kann der cel editor diese json datei noch nicht lesen und die cel logik auslesen und bearbeiten. Bitte erstelle einen Plan wie wir das umsetzen können, in der CEL Editor UI ist wenig Platz. Der Editor müsste die json Datei lesen können, den CEL Teil identifizieren und einlesen können.
Sowie den code dann ändern und speichern können.
Nun brauche ich einen Plan um die Anforderungen möglichst effizient umzusetzen.

JSON Beispieldateien Doku: "\01_Projectplan\Regime V2 JSON Dateien"
